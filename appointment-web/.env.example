# Leave unset in dev: requests go to /api and Vite proxies them to localhost:3000.
# Set this for a deployed build, e.g. the Render URL of appointment-ts.
# The API must then allow this frontend's origin via CORS.
# VITE_API_URL=https://appointment-ts.onrender.com
